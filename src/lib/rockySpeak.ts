/**
 * rocky_transform - Disabled. Now returns original text for basic AI English.
 */

export function rockyTransform(text: string): string {
  // Disabled as per user request to use basic AI English
  return text;
}
